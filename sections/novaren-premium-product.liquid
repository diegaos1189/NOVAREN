{% liquid
  assign product_form_id = 'ProductForm-' | append: section.id
%}

<section class="nvp-product section-{{ section.id }}">
  <div class="nvp-shell nvp-product__grid">
    <div class="nvp-product__gallery" data-nvp-gallery="{{ section.id }}">
      {% if product.media.size > 1 and section.settings.show_thumbnails %}
        <div class="nvp-thumbs" aria-label="Imágenes del producto">
          {% for media in product.media limit: section.settings.media_stack_limit %}
            {% if media.media_type == 'image' %}
              <button
                type="button"
                class="nvp-thumb {% if forloop.first %}is-active{% endif %}"
                data-nvp-thumb
                data-full="{{ media | image_url: width: 1600 }}"
                data-alt="{{ media.alt | default: product.title | escape }}"
                aria-label="Ver imagen {{ forloop.index }} de {{ product.title | escape }}"
                {% if forloop.first %}aria-current="true"{% endif %}
              >
                {{
                  media
                  | image_url: width: 180
                  | image_tag:
                    widths: '90, 140, 180',
                    sizes: '74px',
                    loading: 'lazy',
                    class: 'nvp-thumb__image',
                    alt: media.alt | default: product.title
                }}
              </button>
            {% endif %}
          {% endfor %}
        </div>
      {% endif %}

      <div class="nvp-product__media">
        {% if product.featured_media != blank %}
          {{
            product.featured_media
            | image_url: width: 1600
            | image_tag:
              widths: '480, 720, 960, 1200, 1600',
              sizes: '(min-width: 990px) 48vw, 100vw',
              loading: 'eager',
              fetchpriority: 'high',
              class: 'nvp-product__image',
              data-nvp-main-image: section.id,
              alt: product.featured_media.alt | default: product.title
          }}
        {% endif %}
      </div>
    </div>

    <div class="nvp-product__info">
      <p class="nvp-eyebrow">{{ section.settings.eyebrow | default: product.type }}</p>
      <h1>
        {% if section.settings.custom_title != blank %}
          {{ section.settings.custom_title }}
        {% else %}
          {{ product.title }}
        {% endif %}
      </h1>
      <div class="nvp-price">{{ product.selected_or_first_available_variant.price | money }}</div>

      <div class="nvp-summary">
        {% if section.settings.custom_summary != blank %}
          {{ section.settings.custom_summary }}
        {% elsif product.description != blank %}
          {{ product.description | strip_html | truncatewords: section.settings.summary_words }}
        {% else %}
          {{ section.settings.fallback_summary }}
        {% endif %}
      </div>

      {% if section.settings.show_conversion_panel %}
        <div class="nvp-conversion">
          <div class="nvp-store">
            <div class="nvp-store__seal" aria-hidden="true">
              <svg viewBox="0 0 24 24" role="img">
                <path d="M12 2.8 14.6 5l3.4-.2 1.1 3.2 2.6 2.1-1.4 3.1.7 3.3-3 1.7-1.7 3-3.3-.7-3.1 1.4-2.1-2.6-3.2-1.1.2-3.4L2.8 12 5 9.4l-.2-3.4L8 4.9l2.1-2.6 3.1 1.4Z"/>
                <path d="m8.4 12.2 2.2 2.2 5-5"/>
              </svg>
            </div>
            <div>
              {% if section.settings.trust_kicker != blank %}
                <span>{{ section.settings.trust_kicker }}</span>
              {% endif %}
              {% if section.settings.trust_title != blank %}
                <strong>{{ section.settings.trust_title }}</strong>
              {% endif %}
              {% if section.settings.trust_text != blank %}
                <p>{{ section.settings.trust_text }}</p>
              {% endif %}
            </div>
          </div>

          <div class="nvp-trustbar" aria-hidden="true">
            <i style="width: {{ section.settings.trust_score_percent }}%;"></i>
          </div>

          <div class="nvp-metrics">
            <div>
              <strong>{{ section.settings.metric_1_value }}</strong>
              <span>{{ section.settings.metric_1_label }}</span>
            </div>
            <div>
              <strong>{{ section.settings.metric_2_value }}</strong>
              <span>{{ section.settings.metric_2_label }}</span>
            </div>
            <div>
              <strong>{{ section.settings.metric_3_value }}</strong>
              <span>{{ section.settings.metric_3_label }}</span>
            </div>
          </div>
        </div>

        <div class="nvp-benefits">
          {% if section.settings.benefit_1 != blank %}
            <div>
              <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M3 7h11v8H3z"/><path d="M14 10h4l3 3v2h-7z"/><path d="M7 19a2 2 0 1 0 0-4 2 2 0 0 0 0 4Zm10 0a2 2 0 1 0 0-4 2 2 0 0 0 0 4Z"/></svg>
              <div class="nvp-benefit-text">{{ section.settings.benefit_1 }}</div>
            </div>
          {% endif %}
          {% if section.settings.benefit_2 != blank %}
            <div>
              <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M12 3 5 6v5c0 4.4 2.8 8.4 7 10 4.2-1.6 7-5.6 7-10V6z"/><path d="m8.8 12.2 2.1 2.1 4.5-4.6"/></svg>
              <div class="nvp-benefit-text">{{ section.settings.benefit_2 }}</div>
            </div>
          {% endif %}
          {% if section.settings.benefit_3 != blank %}
            <div>
              <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M4 6h16v12H4z"/><path d="M4 9h16"/><path d="M8 14h4"/></svg>
              <div class="nvp-benefit-text">{{ section.settings.benefit_3 }}</div>
            </div>
          {% endif %}
        </div>
      {% endif %}

      {% form 'product', product, class: 'nvp-form', id: product_form_id %}
        {% if product.has_only_default_variant %}
          <input type="hidden" name="id" value="{{ product.selected_or_first_available_variant.id }}">
        {% else %}
          <label for="Variant-{{ section.id }}">{{ section.settings.variant_label }}</label>
          <select id="Variant-{{ section.id }}" name="id">
            {% for variant in product.variants %}
              <option
                value="{{ variant.id }}"
                {% if variant == product.selected_or_first_available_variant %}selected{% endif %}
                {% unless variant.available %}disabled{% endunless %}
              >
                {{ variant.title }}{% unless variant.available %} - Agotado{% endunless %}
              </option>
            {% endfor %}
          </select>
        {% endif %}

        {% if section.settings.show_quantity %}
          <label for="Quantity-{{ section.id }}">{{ section.settings.quantity_label }}</label>
          <input id="Quantity-{{ section.id }}" type="number" name="quantity" value="1" min="1">
        {% endif %}

        <button type="submit" name="add" {% unless product.selected_or_first_available_variant.available %}disabled{% endunless %}>
          {% if product.selected_or_first_available_variant.available %}
            {{ section.settings.button_label }}
          {% else %}
            {{ section.settings.sold_out_label }}
          {% endif %}
        </button>
        {% if section.settings.cta_note != blank %}
          <p class="nvp-cta-note">{{ section.settings.cta_note }}</p>
        {% endif %}
      {% endform %}

      <div class="nvp-promises">
        {% for block in section.blocks %}
          {% if block.type == 'promise' %}
            <span {{ block.shopify_attributes }}>{{ block.settings.text }}</span>
          {% endif %}
        {% endfor %}
      </div>
    </div>
  </div>

  <div class="nvp-shell nvp-details">
    {% for block in section.blocks %}
      {% if block.type == 'detail' %}
        <article {{ block.shopify_attributes }}>
          <p class="nvp-eyebrow">{{ block.settings.eyebrow }}</p>
          <h2>{{ block.settings.heading }}</h2>
          <p>{{ block.settings.text }}</p>
        </article>
      {% endif %}
    {% endfor %}
  </div>

  {% if section.settings.show_mobile_sticky_cta %}
    <div class="nvp-mobile-cta" aria-label="Compra rápida">
      <div>
        <span>{{ section.settings.mobile_sticky_kicker }}</span>
        <strong>{{ product.selected_or_first_available_variant.price | money }}</strong>
      </div>
      <button
        type="submit"
        name="add"
        form="{{ product_form_id }}"
        {% unless product.selected_or_first_available_variant.available %}disabled{% endunless %}
      >
        {% if product.selected_or_first_available_variant.available %}
          {{ section.settings.mobile_sticky_label }}
        {% else %}
          {{ section.settings.sold_out_label }}
        {% endif %}
      </button>
    </div>
  {% endif %}
</section>

<style>
  .section-{{ section.id }} {
    --ink: #10231c;
    --muted: #657067;
    --green: {{ section.settings.accent_color }};
    --line: rgba(16, 35, 28, .12);
    color: var(--ink);
    background:
      radial-gradient(circle at 16% 8%, rgba(21, 91, 61, .08), transparent 26%),
      linear-gradient(180deg, {{ section.settings.background_top }}, {{ section.settings.background_bottom }});
    padding: {{ section.settings.padding_top }}px 0 {{ section.settings.padding_bottom }}px;
    overflow-x: hidden;
  }

  .section-{{ section.id }} .nvp-shell {
    width: calc(100% - 32px);
    max-width: 1080px;
    margin-inline: auto;
  }

  .section-{{ section.id }} .nvp-product__grid {
    display: grid;
    grid-template-columns: minmax(0, 1.04fr) minmax(360px, .86fr);
    gap: clamp(22px, 4vw, 44px);
    align-items: start;
  }

  .section-{{ section.id }} .nvp-product__gallery {
    display: grid;
    grid-template-columns: {% if product.media.size > 1 and section.settings.show_thumbnails %}86px minmax(0, 1fr){% else %}minmax(0, 1fr){% endif %};
    gap: 16px;
    align-self: start;
    border: 1px solid var(--line);
    border-radius: 8px;
    background: #fff;
    padding: 16px;
    box-shadow: 0 20px 64px rgba(16, 35, 28, .1);
  }

  .section-{{ section.id }} .nvp-product__media {
    position: relative;
    aspect-ratio: 1 / 1;
    border-radius: 8px;
    background: #fff;
    overflow: hidden;
  }

  .section-{{ section.id }} .nvp-product__image {
    width: 100%;
    height: 100%;
    object-fit: {{ section.settings.media_fit }};
    display: block;
    padding: {{ section.settings.media_padding }}px;
    background: #fff;
  }

  .section-{{ section.id }} .nvp-thumbs {
    display: grid;
    align-content: start;
    gap: 10px;
    max-height: min(620px, 72vh);
    overflow-y: auto;
    padding-right: 2px;
  }

  .section-{{ section.id }} .nvp-thumb {
    width: 74px;
    aspect-ratio: 1 / 1;
    border: 1px solid var(--line);
    border-radius: 8px;
    background: #fff;
    padding: 0;
    overflow: hidden;
    cursor: pointer;
    opacity: .68;
    transition: opacity .2s ease, transform .2s ease, border-color .2s ease, box-shadow .2s ease;
  }

  .section-{{ section.id }} .nvp-thumb:hover,
  .section-{{ section.id }} .nvp-thumb.is-active {
    opacity: 1;
    transform: translateY(-1px);
    border-color: color-mix(in srgb, var(--green) 45%, var(--line));
    box-shadow: 0 10px 24px rgba(16, 35, 28, .12);
  }

  .section-{{ section.id }} .nvp-thumb__image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  .section-{{ section.id }} .nvp-product__info {
    position: sticky;
    top: 104px;
    border: 1px solid rgba(16, 35, 28, .1);
    border-radius: 8px;
    background: rgba(255,255,255,.94);
    padding: clamp(22px, 3vw, 30px);
    box-shadow: 0 20px 66px rgba(16, 35, 28, .1);
    backdrop-filter: blur(14px);
  }

  .section-{{ section.id }} .nvp-eyebrow {
    margin: 0 0 12px;
    color: var(--green);
    font-size: {{ section.settings.eyebrow_font_size }}px;
    font-weight: 850;
    letter-spacing: .12em;
    text-transform: uppercase;
  }

  .section-{{ section.id }} h1 {
    margin: 0;
    color: var(--ink);
    font-size: {{ section.settings.title_font_size_desktop }}px;
    line-height: {{ section.settings.title_line_height }}%;
    letter-spacing: 0;
    overflow-wrap: anywhere;
  }

  .section-{{ section.id }} .nvp-price {
    margin-top: 18px;
    color: var(--green);
    font-size: {{ section.settings.price_font_size }}px;
    font-weight: 900;
  }

  .section-{{ section.id }} .nvp-summary {
    margin-top: 16px;
    color: var(--muted);
    font-size: {{ section.settings.summary_font_size_desktop }}px;
    line-height: {{ section.settings.summary_line_height }}%;
  }

  .section-{{ section.id }} .nvp-summary p {
    margin: 0 0 12px;
  }

  .section-{{ section.id }} .nvp-summary p:last-child {
    margin-bottom: 0;
  }

  .section-{{ section.id }} .nvp-conversion {
    margin-top: 26px;
    border: 1px solid rgba(16, 35, 28, .1);
    border-radius: 8px;
    background:
      radial-gradient(circle at 18% 0%, rgba(21, 91, 61, .1), transparent 32%),
      linear-gradient(180deg, rgba(255,255,255,.96), rgba(255,255,255,.84));
    padding: 18px;
    box-shadow: 0 18px 52px rgba(16, 35, 28, .08);
  }

  .section-{{ section.id }} .nvp-store {
    display: grid;
    grid-template-columns: auto minmax(0, 1fr);
    gap: 14px;
    align-items: center;
  }

  .section-{{ section.id }} .nvp-store__seal {
    width: 52px;
    height: 52px;
    display: grid;
    place-items: center;
    border-radius: 999px;
    background: color-mix(in srgb, var(--green) 12%, #ffffff);
    color: var(--green);
  }

  .section-{{ section.id }} .nvp-store__seal svg,
  .section-{{ section.id }} .nvp-benefits svg {
    width: 24px;
    height: 24px;
    fill: none;
    stroke: currentColor;
    stroke-width: 1.8;
    stroke-linecap: round;
    stroke-linejoin: round;
  }

  .section-{{ section.id }} .nvp-store span {
    display: block;
    color: var(--muted);
    font-size: 12px;
    font-weight: 800;
  }

  .section-{{ section.id }} .nvp-store strong {
    display: block;
    margin-top: 2px;
    color: var(--green);
    font-size: {{ section.settings.conversion_title_font_size }}px;
    line-height: 1.18;
  }

  .section-{{ section.id }} .nvp-store p {
    margin: 3px 0 0;
    color: var(--muted);
    font-size: {{ section.settings.conversion_text_font_size }}px;
    line-height: 1.35;
  }

  .section-{{ section.id }} .nvp-trustbar {
    height: 8px;
    margin-top: 16px;
    border-radius: 999px;
    background: linear-gradient(90deg, rgba(245, 96, 74, .16), rgba(255, 211, 104, .18), rgba(21, 91, 61, .18));
    overflow: hidden;
  }

  .section-{{ section.id }} .nvp-trustbar i {
    display: block;
    height: 100%;
    max-width: 100%;
    border-radius: inherit;
    background: linear-gradient(90deg, #8fd6ad, var(--green));
  }

  .section-{{ section.id }} .nvp-metrics {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    margin-top: 14px;
    border-top: 1px solid var(--line);
  }

  .section-{{ section.id }} .nvp-metrics div {
    padding: 14px 10px 0;
    text-align: center;
  }

  .section-{{ section.id }} .nvp-metrics div + div {
    border-left: 1px solid var(--line);
  }

  .section-{{ section.id }} .nvp-metrics strong,
  .section-{{ section.id }} .nvp-metrics span {
    display: block;
  }

  .section-{{ section.id }} .nvp-metrics strong {
    color: var(--ink);
    font-size: {{ section.settings.metric_value_font_size }}px;
    line-height: 1.05;
  }

  .section-{{ section.id }} .nvp-metrics span {
    margin-top: 5px;
    color: var(--muted);
    font-size: {{ section.settings.metric_label_font_size }}px;
    line-height: 1.25;
  }

  .section-{{ section.id }} .nvp-benefits {
    display: grid;
    gap: 10px;
    margin-top: 14px;
    border-radius: 8px;
    background: #f4f6f5;
    padding: 16px;
  }

  .section-{{ section.id }} .nvp-benefits div {
    display: grid;
    grid-template-columns: auto minmax(0, 1fr);
    gap: 10px;
    align-items: start;
    color: var(--green);
  }

  .section-{{ section.id }} .nvp-benefits svg {
    width: 19px;
    height: 19px;
    margin-top: 1px;
  }

  .section-{{ section.id }} .nvp-benefit-text {
    color: var(--ink);
    font-size: {{ section.settings.benefit_font_size }}px;
    line-height: 1.42;
  }

  .section-{{ section.id }} .nvp-benefit-text p {
    margin: 0;
    color: var(--ink);
  }

  .section-{{ section.id }} .nvp-benefit-text strong {
    font-weight: 900;
  }

  .section-{{ section.id }} .nvp-form {
    display: grid;
    gap: 12px;
    margin-top: 18px;
  }

  .section-{{ section.id }} .nvp-form label {
    color: var(--ink);
    font-size: {{ section.settings.form_label_font_size }}px;
    font-weight: 850;
  }

  .section-{{ section.id }} .nvp-form select,
  .section-{{ section.id }} .nvp-form input {
    width: 100%;
    min-height: 48px;
    border: 1px solid var(--line);
    border-radius: 8px;
    padding: 0 14px;
    background: #fff;
    color: var(--ink);
    font: inherit;
  }

  .section-{{ section.id }} .nvp-form button {
    min-height: 62px;
    border: 0;
    border-radius: 8px;
    background: linear-gradient(135deg, var(--green), #0d3f2c);
    color: #fff;
    font: inherit;
    font-size: {{ section.settings.button_font_size }}px;
    font-weight: 900;
    cursor: pointer;
    box-shadow: 0 18px 42px rgba(21, 91, 61, .28);
    transition: transform .22s ease, box-shadow .22s ease, filter .22s ease;
  }

  .section-{{ section.id }} .nvp-form button:hover {
    transform: translateY(-2px);
    filter: saturate(1.08) brightness(1.02);
    box-shadow: 0 22px 48px rgba(21, 91, 61, .32);
  }

  .section-{{ section.id }} .nvp-form button:disabled {
    opacity: .55;
    cursor: not-allowed;
  }

  .section-{{ section.id }} .nvp-cta-note {
    margin: -4px 0 0;
    color: var(--muted);
    font-size: 13px;
    line-height: 1.4;
    text-align: center;
  }

  .section-{{ section.id }} .nvp-promises {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    margin-top: 20px;
  }

  .section-{{ section.id }} .nvp-promises span {
    border: 1px solid var(--line);
    border-radius: 999px;
    padding: 8px 12px;
    background: #fff;
    color: var(--muted);
    font-size: {{ section.settings.promise_font_size }}px;
    font-weight: 850;
  }

  .section-{{ section.id }} .nvp-details {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 18px;
    margin-top: clamp(46px, 7vw, 86px);
  }

  .section-{{ section.id }} .nvp-details article {
    border: 1px solid var(--line);
    border-radius: 8px;
    background: rgba(255,255,255,.82);
    padding: clamp(22px, 4vw, 34px);
    box-shadow: 0 20px 60px rgba(16, 35, 28, .08);
  }

  .section-{{ section.id }} .nvp-details h2 {
    margin: 0;
    color: var(--ink);
    font-size: {{ section.settings.detail_title_font_size }}px;
    line-height: {{ section.settings.detail_title_line_height }}%;
    letter-spacing: 0;
  }

  .section-{{ section.id }} .nvp-details p:not(.nvp-eyebrow) {
    margin: 14px 0 0;
    color: var(--muted);
    font-size: {{ section.settings.detail_text_font_size }}px;
    line-height: {{ section.settings.detail_text_line_height }}%;
  }

  .section-{{ section.id }} .nvp-mobile-cta {
    display: none;
  }

  @media (max-width: 989px) {
    .section-{{ section.id }} .nvp-product__grid,
    .section-{{ section.id }} .nvp-details {
      grid-template-columns: minmax(0, 1fr);
    }

    .section-{{ section.id }} .nvp-product__info {
      position: static;
    }

    .section-{{ section.id }} .nvp-product__gallery {
      grid-template-columns: minmax(0, 1fr);
    }

    .section-{{ section.id }} .nvp-thumbs {
      order: 2;
      grid-auto-flow: column;
      grid-auto-columns: 72px;
      overflow-x: auto;
      overflow-y: hidden;
      max-height: none;
      padding: 2px 0 8px;
    }
  }

  @media (max-width: 640px) {
    .section-{{ section.id }} {
      padding-bottom: calc({{ section.settings.padding_bottom }}px + 86px);
    }

    .section-{{ section.id }} .nvp-shell {
      width: calc(100% - 24px);
      max-width: 330px;
      margin-left: 12px;
      margin-right: auto;
    }

    .section-{{ section.id }} h1 {
      font-size: {{ section.settings.title_font_size_mobile }}px;
    }

    .section-{{ section.id }} .nvp-summary {
      font-size: {{ section.settings.summary_font_size_mobile }}px;
    }

    .section-{{ section.id }} .nvp-metrics {
      grid-template-columns: minmax(0, 1fr);
      gap: 10px;
    }

    .section-{{ section.id }} .nvp-metrics div {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 14px;
      padding: 10px 0 0;
      text-align: left;
    }

    .section-{{ section.id }} .nvp-metrics div + div {
      border-left: 0;
      border-top: 1px solid var(--line);
    }

    .section-{{ section.id }} .nvp-mobile-cta {
      position: fixed;
      left: 10px;
      right: 10px;
      bottom: 10px;
      z-index: 40;
      display: grid;
      grid-template-columns: minmax(0, .86fr) minmax(0, 1.14fr);
      align-items: center;
      gap: 10px;
      border: 1px solid rgba(255,255,255,.6);
      border-radius: 12px;
      background: rgba(255,255,255,.94);
      box-shadow: 0 18px 52px rgba(16,35,28,.22);
      padding: 10px;
      backdrop-filter: blur(16px);
    }

    .section-{{ section.id }} .nvp-mobile-cta span,
    .section-{{ section.id }} .nvp-mobile-cta strong {
      display: block;
      min-width: 0;
    }

    .section-{{ section.id }} .nvp-mobile-cta span {
      color: var(--muted);
      font-size: 11px;
      font-weight: 850;
      text-transform: uppercase;
      letter-spacing: .04em;
    }

    .section-{{ section.id }} .nvp-mobile-cta strong {
      margin-top: 2px;
      color: var(--green);
      font-size: 18px;
      line-height: 1.1;
    }

    .section-{{ section.id }} .nvp-mobile-cta button {
      min-height: 48px;
      border: 0;
      border-radius: 10px;
      background: linear-gradient(135deg, var(--green), #0d3f2c);
      color: #fff;
      font: inherit;
      font-size: 14px;
      font-weight: 900;
      cursor: pointer;
    }

    .section-{{ section.id }} .nvp-mobile-cta button:disabled {
      opacity: .55;
    }
  }
</style>

<script>
  (() => {
    const root = document.querySelector('[data-nvp-gallery="{{ section.id }}"]');
    if (!root) return;

    const mainImage = root.querySelector('[data-nvp-main-image="{{ section.id }}"]');
    const thumbs = [...root.querySelectorAll('[data-nvp-thumb]')];
    if (!mainImage || !thumbs.length) return;

    thumbs.forEach((thumb) => {
      thumb.addEventListener('click', () => {
        const nextSrc = thumb.dataset.full;
        const nextAlt = thumb.dataset.alt || mainImage.alt;
        if (!nextSrc) return;

        mainImage.src = nextSrc;
        mainImage.removeAttribute('srcset');
        mainImage.alt = nextAlt;

        thumbs.forEach((item) => {
          item.classList.remove('is-active');
          item.removeAttribute('aria-current');
        });
        thumb.classList.add('is-active');
        thumb.setAttribute('aria-current', 'true');
      });
    });
  })();
</script>

{% schema %}
{
  "name": "Novaren producto premium",
  "settings": [
    { "type": "text", "id": "eyebrow", "label": "Etiqueta superior", "default": "Bienestar" },
    { "type": "text", "id": "custom_title", "label": "Título personalizado", "info": "Déjalo vacío para usar el título real del producto." },
    { "type": "richtext", "id": "custom_summary", "label": "Párrafo personalizado", "info": "Déjalo vacío para usar la descripción del producto." },
    { "type": "textarea", "id": "fallback_summary", "label": "Resumen si no hay descripción", "default": "Producto seleccionado por Novaren para acompañar rutinas saludables, autocuidado y bienestar diario." },
    { "type": "range", "id": "summary_words", "label": "Palabras del resumen", "min": 18, "max": 80, "step": 2, "default": 42 },
    { "type": "header", "content": "Imagen principal" },
    {
      "type": "select",
      "id": "media_fit",
      "label": "Ajuste de imagen",
      "default": "cover",
      "options": [
        { "value": "cover", "label": "Llenar el cuadro" },
        { "value": "contain", "label": "Mostrar completa" }
      ]
    },
    { "type": "range", "id": "media_padding", "label": "Espacio interno de imagen", "min": 0, "max": 44, "step": 2, "unit": "px", "default": 0 },
    { "type": "checkbox", "id": "show_thumbnails", "label": "Mostrar miniaturas tipo carrusel", "default": true },
    { "type": "range", "id": "media_stack_limit", "label": "Cantidad de miniaturas", "min": 2, "max": 12, "step": 1, "default": 8 },
    { "type": "header", "content": "Tamaños de texto" },
    { "type": "range", "id": "eyebrow_font_size", "label": "Etiqueta superior", "min": 10, "max": 18, "step": 1, "unit": "px", "default": 12 },
    { "type": "range", "id": "title_font_size_desktop", "label": "Título en escritorio", "min": 34, "max": 76, "step": 1, "unit": "px", "default": 54 },
    { "type": "range", "id": "title_font_size_mobile", "label": "Título en móvil", "min": 26, "max": 48, "step": 1, "unit": "px", "default": 32 },
    { "type": "range", "id": "title_line_height", "label": "Interlineado del título", "min": 90, "max": 125, "step": 1, "unit": "%", "default": 104 },
    { "type": "range", "id": "price_font_size", "label": "Precio", "min": 20, "max": 42, "step": 1, "unit": "px", "default": 34 },
    { "type": "range", "id": "summary_font_size_desktop", "label": "Párrafo en escritorio", "min": 14, "max": 24, "step": 1, "unit": "px", "default": 17 },
    { "type": "range", "id": "summary_font_size_mobile", "label": "Párrafo en móvil", "min": 13, "max": 21, "step": 1, "unit": "px", "default": 16 },
    { "type": "range", "id": "summary_line_height", "label": "Interlineado del párrafo", "min": 130, "max": 190, "step": 5, "unit": "%", "default": 165 },
    { "type": "range", "id": "button_font_size", "label": "Botón de compra", "min": 14, "max": 22, "step": 1, "unit": "px", "default": 16 },
    { "type": "range", "id": "promise_font_size", "label": "Promesas de confianza", "min": 11, "max": 18, "step": 1, "unit": "px", "default": 13 },
    { "type": "range", "id": "form_label_font_size", "label": "Etiquetas del formulario", "min": 11, "max": 17, "step": 1, "unit": "px", "default": 13 },
    { "type": "range", "id": "detail_title_font_size", "label": "Títulos de bloques inferiores", "min": 22, "max": 42, "step": 1, "unit": "px", "default": 30 },
    { "type": "range", "id": "detail_title_line_height", "label": "Interlineado de títulos inferiores", "min": 95, "max": 130, "step": 1, "unit": "%", "default": 108 },
    { "type": "range", "id": "detail_text_font_size", "label": "Párrafos de bloques inferiores", "min": 14, "max": 22, "step": 1, "unit": "px", "default": 16 },
    { "type": "range", "id": "detail_text_line_height", "label": "Interlineado de bloques inferiores", "min": 130, "max": 190, "step": 5, "unit": "%", "default": 165 },
    { "type": "header", "content": "Formulario y compra" },
    { "type": "text", "id": "variant_label", "label": "Etiqueta variantes", "default": "Presentación" },
    { "type": "checkbox", "id": "show_quantity", "label": "Mostrar cantidad", "default": true },
    { "type": "text", "id": "quantity_label", "label": "Etiqueta cantidad", "default": "Cantidad" },
    { "type": "text", "id": "button_label", "label": "Texto botón compra", "default": "Agregar al carrito" },
    { "type": "text", "id": "sold_out_label", "label": "Texto agotado", "default": "Agotado" },
    { "type": "textarea", "id": "cta_note", "label": "Nota bajo el botón", "default": "Compra protegida. Revisaremos tu pedido y te acompañaremos en el proceso." },
    { "type": "checkbox", "id": "show_mobile_sticky_cta", "label": "Mostrar botón fijo en móvil", "default": true },
    { "type": "text", "id": "mobile_sticky_kicker", "label": "Texto pequeño del botón fijo", "default": "Compra rápida" },
    { "type": "text", "id": "mobile_sticky_label", "label": "Texto botón fijo móvil", "default": "Comprar ahora" },
    { "type": "header", "content": "Caja premium de conversión" },
    { "type": "checkbox", "id": "show_conversion_panel", "label": "Mostrar caja premium de confianza", "default": true },
    { "type": "text", "id": "trust_kicker", "label": "Texto superior de confianza", "default": "Nuevo en Novaren | Disponible" },
    { "type": "text", "id": "trust_title", "label": "Título de confianza", "default": "Compra respaldada por Novaren" },
    { "type": "textarea", "id": "trust_text", "label": "Texto de confianza", "default": "Atención clara, compra segura y seguimiento del pedido desde nuestros canales oficiales." },
    { "type": "range", "id": "trust_score_percent", "label": "Nivel visual de confianza", "min": 20, "max": 100, "step": 5, "unit": "%", "default": 90 },
    { "type": "text", "id": "metric_1_value", "label": "Métrica 1 valor", "default": "Seguro" },
    { "type": "text", "id": "metric_1_label", "label": "Métrica 1 texto", "default": "Pago y datos protegidos" },
    { "type": "text", "id": "metric_2_value", "label": "Métrica 2 valor", "default": "Activo" },
    { "type": "text", "id": "metric_2_label", "label": "Métrica 2 texto", "default": "Soporte al cliente" },
    { "type": "text", "id": "metric_3_value", "label": "Métrica 3 valor", "default": "Colombia" },
    { "type": "text", "id": "metric_3_label", "label": "Métrica 3 texto", "default": "Envíos nacionales" },
    { "type": "richtext", "id": "benefit_1", "label": "Beneficio 1", "default": "<p><strong>Envío gratis desde $149.900</strong> en Colombia, según condiciones vigentes.</p>" },
    { "type": "richtext", "id": "benefit_2", "label": "Beneficio 2", "default": "<p><strong>Compra garantizada.</strong> Si hay una novedad con tu pedido, te ayudamos a revisarla.</p>" },
    { "type": "richtext", "id": "benefit_3", "label": "Beneficio 3", "default": "<p><strong>Pago contra entrega disponible</strong> cuando aplique para tu ciudad o método de envío.</p>" },
    { "type": "range", "id": "conversion_title_font_size", "label": "Título caja de confianza", "min": 16, "max": 26, "step": 1, "unit": "px", "default": 19 },
    { "type": "range", "id": "conversion_text_font_size", "label": "Texto caja de confianza", "min": 12, "max": 18, "step": 1, "unit": "px", "default": 14 },
    { "type": "range", "id": "metric_value_font_size", "label": "Valor de métricas", "min": 15, "max": 26, "step": 1, "unit": "px", "default": 19 },
    { "type": "range", "id": "metric_label_font_size", "label": "Texto de métricas", "min": 10, "max": 15, "step": 1, "unit": "px", "default": 12 },
    { "type": "range", "id": "benefit_font_size", "label": "Texto de beneficios", "min": 13, "max": 18, "step": 1, "unit": "px", "default": 15 },
    { "type": "color", "id": "accent_color", "label": "Color acento", "default": "#155b3d" },
    { "type": "color", "id": "background_top", "label": "Fondo superior", "default": "#fbfaf5" },
    { "type": "color", "id": "background_bottom", "label": "Fondo inferior", "default": "#ffffff" },
    { "type": "range", "id": "padding_top", "label": "Espacio superior", "min": 20, "max": 100, "step": 4, "default": 72 },
    { "type": "range", "id": "padding_bottom", "label": "Espacio inferior", "min": 40, "max": 140, "step": 4, "default": 96 }
  ],
  "blocks": [
    {
      "type": "promise",
      "name": "Promesa de confianza",
      "settings": [
        { "type": "text", "id": "text", "label": "Texto", "default": "Compra segura" }
      ]
    },
    {
      "type": "detail",
      "name": "Bloque informativo",
      "settings": [
        { "type": "text", "id": "eyebrow", "label": "Etiqueta", "default": "Rutina Novaren" },
        { "type": "text", "id": "heading", "label": "Título", "default": "Bienestar pensado para tu día a día" },
        { "type": "textarea", "id": "text", "label": "Texto", "default": "Integra este producto como complemento de hábitos simples: buena hidratación, alimentación equilibrada, movimiento suave y descanso." }
      ]
    }
  ],
  "presets": [
    {
      "name": "Novaren producto premium",
      "blocks": [
        { "type": "promise", "settings": { "text": "Compra segura" } },
        { "type": "promise", "settings": { "text": "Envíos en Colombia" } },
        { "type": "promise", "settings": { "text": "Atención personalizada" } },
        { "type": "detail", "settings": { "eyebrow": "Rutina Novaren", "heading": "Bienestar pensado para tu día a día", "text": "Integra este producto como complemento de hábitos simples: buena hidratación, alimentación equilibrada, movimiento suave y descanso. Si tienes una condición médica, estás en embarazo, lactancia o tomas medicamentos, consulta con un profesional de salud." } },
        { "type": "detail", "settings": { "eyebrow": "Guía rápida", "heading": "Cómo elegir con confianza", "text": "Revisa la descripción, presentación y modo de uso del producto. Novaren selecciona productos para que comprar bienestar online sea claro, cómodo y seguro." } }
      ]
    }
  ]
}
{% endschema %}
